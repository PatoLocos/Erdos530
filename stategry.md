Strategy
The axiom states: AdmitsTwoToOneMap A S — there exists a function f : ℕ → ℕ × ℕ mapping each x ∈ A \ S to a pair in S ×ˢ S, with each fiber having size ≤ 2.

The plan:

Define canonicalPair S x — the lex-smallest (a,b) in S × S witnessing that x is blocked

Verify this maps A \ S into S ×ˢ S and establish that each fiber contains at most 2 elements by separating Type 2 elements (where 2x = a + b) from Type 1-only elements, since KSS guarantees at most one element of each type maps to any given pair.

For a blocked element, I need to define f(x) as the lexicographically smallest pair from S × S satisfying either the Type 1 or Type 2 condition, which ensures a well-defined function that respects the fiber size bound.

The key insight is that at most one Type 2 element can map to any pair (a,b) since x = (a+b)/2 is unique, and at most one Type 1-only element can map to the same pair because if two such elements shared a canonical pair, their difference would equal a difference of elements in S, creating a contradiction with the blocking structure. If c₁ equals c₂ then x₁ = x₂ immediately, but if they differ, then x₁ + c₂ = x₂ + c₁ = a + b, which means the same sum appears twice in S+S with different representations—this is the key contradiction that forces the canonical pairs to be distinct. I need to reconsider the KSS argument more carefully—if three blocked elements share the same canonical pair, then one must be Type 2, and this works because each element xᵢ satisfies xᵢ + cᵢ = a + b for distinct values cᵢ in S, which means they can't all be equal. I see that x₁ + c₁ = a + b and x₂ + c₂ = a + b from the given constraints, so x₁ + c₁ = x₂ + c₂, which means x₁ - x₂ = c₂ - c₁. I'm recognizing that the key constraint is x₁ + c₁ = x₂ + c₂, which means x₁ - x₂ = c₂ - c₁, and this relationship lets me express x₁ + c₂ in terms of the sum (a+b) plus the difference between the constants.

Since S is a Sidon set and c₁, c₂ are elements in it, I need to check whether x₁ + c₂ creates a collision with any existing pairwise sums in S.

The KSS argument works by charging each blocked element to a pair (a,b): Type 2 elements are uniquely determined by their pair, but Type 1 elements x where x + c = a + b can have multiple x values for the same pair depending on which c ∈ S we use, so I need to count how many such x can be charged to a single pair.

For Type 1-only elements with the same canonical pair (a,b), if two distinct elements x₁ and x₂ both satisfy x₁ + c₁ = a + b and x₂ + c₂ = a + b with c₁, c₂ ∈ S and c₁ ≠ c₂, then the Sidon property of S constrains what's possible.

Actually, I think I'm overcomplicating this—let me look back at the original KSS argument and consider a more direct approach that just counts fibers without worrying about canonical pairs being injective.

For Type 2 elements, the map is automatically injective since 2x uniquely determines x from any pair (a,b) with 2x = a+b. For Type 1-only elements sharing the same canonical pair (a,b), if x₁ + c₁ = a + b and x₂ + c₂ = a + b for elements in S, then x₁ and x₂ differ by c₂ - c₁, which constrains how many such elements can exist. I'm substituting the expressions for x₁ and x₂ to find what x₁ + c₂ and x₂ + c₁ equal, noting that these sums are symmetric around a + b. If x₁ + c₂ can be expressed as a sum of two elements from S, then I've found a witness pair that makes x₁ a Type 1 element.

But I'm realizing this approach requires knowing whether x₁ + c₂ actually lies in S + S, which isn't guaranteed. Let me reconsider the overall strategy—maybe there's a simpler path using the existing cubic bound result with some refinement, or I need to look more carefully at what the proof structure for AdmitsTwoToOneMap actually requires.

For the map f, I need to handle blocked elements by their type: either they're sums of two elements from S, or they fit some other blocked form, and I should map each case appropriately to ensure the preimage of any pair has at most 2 elements. -only element" part, I need to show that if two distinct elements x, y ∈ A \ S both map to (a,b) via Type 1, then we get a contradiction — likely by showing that one of them would actually be Type 2, or that the lex-minimality condition fails for one of them. Since x₁ + c₂ exceeds a + b, I need to determine whether this sum actually appears in S + S, and I'm realizing I should reconsider the structure of collisions more carefully rather than pursuing this particular calculation.

Let me try partitioning A \ S into Type 2 elements (which appear in multiple collision equations) and Type 1-only elements (which appear in exactly one), since Type 2 is already bounded by |S|². For Type 1-only elements, each one witnesses a collision x + c = a + b for some a,b,c ∈ S, and I can map x to a witness pair (a,b), though the challenge is that different values of c might yield different pairs for the same x.

The key insight is to map each Type 1-only element x to the pair (c, σ) where σ = a + b is the sum from the collision equation—this makes the map injective since x = σ - c is uniquely determined by (c, σ). The codomain S × sumset(S) has size at most |S|³, which is more than enough to bound |Type 1-only| ≤ |S|². The fiber size for a fixed pair is at most |S|, giving a cubic bound again. The key insight is using canonical pairs to reduce the fiber to size 1 on Type 1-only elements. I'm reconsidering the existing code structure—it defines a two-to-one map into S ×ˢ S, so if I can show both Type 2 and Type 1-only inject separately into this space, I can combine them.

For Type 2, the injection maps each element to its witness pair, which is injective since the pair determines the element uniquely. For Type 1-only, I need to construct an injection using canonical pairs. I need to shift strategy and use the cardinality argument directly: find an injection from type1Only into S × S by mapping each x to the lexicographically minimal pair (a, b) where x + c = a + b for some c ∈ S, then verify this map is injective.

The key is showing that two distinct type1Only elements can't map to the same pair—if x₁ ≠ x₂ both map to (a, b), then x₁ + c₁ = x₂ + c₂ = a + b for some c₁, c₂ ∈ S, which forces c₁ ≠ c₂ and leads to a contradiction since x₁, x₂ ∉ S. Since x₁ - x₂ = c₂ - c₁ from the collision equation, I can show that x₁ + c₂ - (x₂ + c₁) = 2(c₂ - c₁) > 0, meaning x₁ + c₂ > x₂ + c₁.

Now I need to examine the KSS paper's argument more carefully—specifically how three distinct elements mapping to the same pair leads to a Type 2 contradiction, so let me look at the referenced summary to understand what additional constraint I'm missing. The witness pair (a,b) must be lexicographically minimal for x₁, so any other representation like x₁ + c₂ wouldn't yield a smaller pair. But I'm not even certain x₁ + c₂ lies in S + S. Let me refocus on the core argument: if two distinct Type 1-only elements x₁ and x₂ both witness the same pair (a,b), then from x₁ + c₁ = a + b = x₂ + c₂, I can derive that one of them must actually be Type 2, contradicting the assumption. I'm reconsidering the approach entirely—instead of using canonical witnesses, I should map each Type 1-only element x to a pair (c, σ) where c is from S and σ = x + c, which gives an injection since x is uniquely recoverable as σ - c. The codomain would be S × sumset(S), which has manageable size. I'm trying different injection strategies but running into issues—mapping to (c, a) fails because different x values can share the same pair with different b values. Let me step back and think about what I can actually prove, then work backwards from there, maybe using ordered pairs in a more direct way.

Actually, I realize the key is to map Type 1-only elements x to (c, x+c) where c ∈ S—this is injective since c and the sum x+c uniquely determine x, and the codomain is S × sumset(S), which gives me a quadratic bound.

To push this to a true quadratic bound in k², I need to use the KSS canonical pair trick, which selects a specific pair from each sumset representation to map into S × S directly, keeping the fiber size at most 1 for Type 1-only elements.

Let me formalize this by defining witness pairs and working through the case analysis. The key insight is that at most one Type 1-only element can share the same canonical pair, because if two distinct Type 1-only elements x₁ and x₂ both had canonical pair (a,b), we'd derive a contradiction from the Sidon property using the equations x₁ + c₁ = a + b and x₂ + c₂ = a + b with c₁ ≠ c₂. If three distinct Type 1-only elements share the same canonical pair, their corresponding S-elements must be distinct, and I can use the Sidon property on these three pairs to derive a contradiction—this is the key constraint that limits how many Type 1-only elements can exist. I'm exploring whether the sums x₁ + c₂ and x₂ + c₁ have a relationship within S+S, and I notice that adding them together gives (x₁ + c₁) + (x₂ + c₂), which connects back to the known collision. If σ₁₂ is in S + S as some α + β, then x₁ would be witnessed by that pair too, but since σ₁₂ > σ when c₂ > c₁, the pair (α, β) could be lexicographically larger than (a, b) without creating a contradiction. For the third element x₃ with c₃ ∈ S where x₃ + c₃ = a + b, I need to examine all the cross-sums x_i + c_j where i ≠ j. I'm stuck trying to show 2x₁ is in S + S directly, so let me pivot to a simpler approach—proving the quadratic bound |A \ S| ≤ 2|S|² without relying on the 2-to-1 map. I'll use double counting on collisions: each element outside S triggers at least one collision when inserted, and each collision involves x with a pair or triple from S, where ordered triples from S can produce at most one Type 1 element. The issue is that multiple (σ, c) pairs can produce the same x, so I need to bound how many times each element gets counted—since each x has at most |S| witnesses (one for each choice of c), this gives the bound |A\S| ≤ |S|².

But the direct counting approach maxes out at cubic without a canonical pair construction, so I should instead map each x ∈ A \ S to a witness based on its type: if x is Type 2, pick any (a,b) ∈ S² with 2x = a+b, otherwise use a different canonical representation. For a Sidon set, if I fix an element a, the sums a + b for varying b are all distinct, but that's trivial. What actually matters is that for any given sum σ, there's at most one unordered pair {a, b} achieving it. I'm going in circles with the map construction, so let me pivot to proving the result directly through an alternative characterization instead.

I'll show (A \ S).card ≤ 2|S|² by decomposing it into blocked elements and type-1-only elements, where I already know the blocked count is bounded by |S|². For the type-1-only elements, I need to construct an injective map from them into S × S by assigning each element to the lexicographically minimal pair (a, b) such that x + c = a + b for some c in S, which would establish the bound.

Actually, there's a cleaner path: each sum σ = a + b can block at most |S| - 1 elements as type-1-only, since for a fixed σ the type-1-only blockings come from x = σ - c where c ranges over S excluding a and b, giving at most |S| candidates per sum.

With at most k(k+1)/2 distinct sums for a Sidon set of size k, this gives an upper bound of k³, but the problem is that the same element x gets blocked by multiple sums, so I'm overcounting.

I think I need to commit to using the canonical pair approach—assigning each type-1-only element to its unique minimal pair—rather than trying to bound the blockings directly.

Actually, let me try a different strategy: instead of bounding fibers, I'll count triples (x, c, σ) where x is type-1-only, c ∈ S, and x + c equals some sum σ. Since each (c, σ) pair determines at most one x, and there are at most |S|³ such pairs, this gives |type1Only| ≤ |S|³.

To improve this bound, I'd need to show each x has multiple witnesses—that is, for many different c values, x + c lands in the sumset. But requiring x + c ∈ sumset(S) for all c ∈ S would make x fully compatible with S, which contradicts the type-1-only definition. Even showing each x has just 2 witnesses isn't straightforward.

The counting approach seems to be hitting a wall, so I should pivot back to the canonical pair strategy. Let me try formalizing this in Lean by defining witness pairs for each blocked element x as the set of all (a,b) ∈ S × S that arise from either x + c = a + b for some c ∈ S, or 2x = a + b, then mapping x to the lexicographically minimal pair.

Since everything is finite and decidable, I can construct the witness pairs as a filtered finset, then use the lexicographic order on ℕ × ℕ to extract the minimum element. The key is making sure Lean recognizes the right ordering—I need to use the lexicographic product order rather than the default componentwise order. The key insight is that for Type 1-only elements, the canonical pair must be injective, which the lex-min property guarantees—each blocked element gets a unique minimal witness pair, ensuring at most 2 elements map to any given pair in the image. When c₂ < c₁, the sum x₁ + c₂ falls below a + b, and if this sum is itself in S + S, it would generate a contradiction with the uniqueness of the canonical pair for that value. all witness pairs for x. For Type 1-only elements, if two distinct values share the same canonical pair, their cross-sums would exceed or fall short of the minimum sum, contradicting minimality. This means at most one Type 1-only element can have any given canonical pair. So for x₂ to be blocked, we need a witness pair (a,b) where both a and b are in S, and either x₂ + c = a + b for some c ∈ S, or 2x₂ = a + b. The key constraint is that x₂ + c₁ must actually be expressible as a sum of two S-elements for it to create a valid collision. If x₂ + c₁ isn't in S + S, then this approach falls apart. I need to reconsider the KSS argument more carefully—let me go back to the original lemma and think through what it actually says about picking witnesses for elements outside S. For Type 1 elements, different choices of c ∈ S yield different x values, so I could have up to |S| preimages mapping to (a,b)—but to keep the fiber size bounded at 2, I need to impose a canonical pair property that restricts which (a,b) pairs are actually in the image. Let me reconsider what the KSS argument actually requires and work through a concrete construction of such a map. For the minimum-sum property to hold, there can't be any pair from S × S with a smaller sum that witnesses x, either through the additive condition (some c' ∈ S where x + c' equals that pair's sum) or the doubling condition (2x equals that pair's sum). If 2x were smaller than σ and in S + S, it would contradict the minimality of (a,b) as a witness.

Rather than continuing down this path, I should take a step back and use a cleaner approach—defining the function directly with a minimum operation and working through the proof more systematically. The real breakthrough is recognizing that I can construct an injection from type-1-only elements into S × sumset(S) by mapping each x to its minimum witnessing pair (c_x, σ_x), where c_x is the unique smallest element of S that witnesses x. For a Sidon set, each sum appears uniquely, so the sumset has size roughly |S|²/2, but partitioning type1Only by minimum witness and mapping each partition into the sumset still yields a cubic bound—I need a different approach to tighten this to quadratic.

The key is showing that the map from type1Only to S × S via canonical (lex-minimal) witness pairs is injective, which would immediately give |type1Only| ≤ |S|². Let me verify this injectivity directly: if two distinct type1-only elements share the same lex-minimal witness pair, I can derive a contradiction from the minimality condition. I'm checking whether x₁ + c₂ creates a collision in S + S by substituting x₁ = a + b - c₁, which gives x₁ + c₂ = a + b + (c₂ - c₁), but this doesn't immediately reveal whether it collides with any pair sum from S. I can see that x₁ + x₂ equals 2(a+b) minus the sum from S + S, but this relationship doesn't directly show that x₁ + x₂ itself belongs to S + S, which is where I'm getting stuck.

Let me work through a concrete example with S = {1, 2, 5, 11} to see what's actually happening. Taking σ = 12 from 1 + 11, I can check which elements x = 12 - c are in S for each c ∈ S, which tells me which potential Type 1 elements are available or blocked.

Now I need to verify whether (1, 11) is actually the lexicographically minimal witness pair for x = 10 by finding all pairs from S×S that sum to 10 + c for some c ∈ S, but it looks like no such pairs exist in this set. So 10 is actually blocked because 10 + 2 = 12, and 12 can be expressed as 1 + 11 from the set. The pair (1, 11) witnesses this with c = 2. I'm checking if there's a lexicographically smaller pair that would also block 10, which would require 10 + c' = 1 + b' for some c' in S and b' < 11. For x = 7, the pair (1, 11) works as a witness since 7 + 5 = 12 = 1 + 11. Checking other possible pairs with elements from S, I find that (1, 11) is the only valid witness pair for this value as well.

This is striking—both x = 10 and x = 7 map to the same canonical pair (1, 11), which means two different Type 1-only elements share the same witness pair. Let me verify whether either of these values could actually be Type 2 by checking if they can be expressed as the sum of two elements in S.

For x = 10, I need 20 = a + b with a, b ∈ S = {1, 2, 5, 11}, but the possible sums are 3, 6, 12, 7, 13, 16—none equal 20. For x = 7, I need 14 = a + b, and again no pair from S sums to 14. So both are genuinely Type 1-only, confirming that the fiber over (1, 11) contains at least two elements. This doesn't violate the overall bound of 2 since that applies to the combined count of Type 1-only and Type 2 elements mapping to each pair.

But this contradicts what I thought I was proving—that each pair has at most one Type 1-only element. The actual constraint must be different: the total fiber size (Type 1-only plus Type 2) is bounded by 2, not that Type 1-only is injective. I need to reconsider what the KSS argument actually establishes about how elements distribute across pairs. For a given pair (a,b) with sum σ, there's at most one Type 2 element (when σ is even and σ/2 is in A\S), and the Type 1-only elements are values x = σ - c where c is in S but not in {a,b}, and x itself isn't in S or Type 2—so I need to verify the total fiber size stays bounded by 2.

The key insight is that the canonical pair matters: even if multiple elements share the same witness pair, they might map to different canonical pairs depending on whether each has a lexicographically smaller witness through another pair, which is what keeps the fiber size controlled in my example where both x = 7 and x = 10 map to (1,11) as their canonical pair.

Now I'm wondering if the fiber can exceed 2—specifically, whether three elements from S could all produce values with the same canonical pair (a,b), which would require checking if their witness pairs remain consistent under the canonical pair definition. I'm working through a counting argument: each pair in S can create at most k-2 Type 1 blocked elements, and by double-counting the total sponsorships against distinct blocked elements, I need to show that each canonical pair sponsors at most 2 elements to keep the bound tight. The example demonstrates this bound is achievable.

Now I need to verify whether a canonical pair could sponsor 3 elements instead—the KSS argument claims this leads to a contradiction by examining what happens when three Type 1-only elements share the same canonical pair and their sums with different elements of S. Let me verify the sums work out: x₁ + c₂ and x₂ + c₁ both equal σ when adjusted for the differences between the constants. With three elements, the cross-sums become more complex, and I'm realizing this approach might be getting unwieldy.

Instead of continuing down this path, let me step back and focus on what actually needs proving: that the cardinality of A \ S is bounded by 2 times the square of S's cardinality. I can do this more directly by combining the already-proven bounds on the blocked type-2 elements and the type-1-only elements.

For the type-1-only elements, I need an injection into S², but my earlier example revealed that the canonical pair approach doesn't work because multiple type-1-only elements can map to the same pair. This means I need to use the stronger constraint that the total fiber size across all pairs is at most 2, rather than trying to prove injectivity directly. I'm realizing the canonical pair map with lexicographic minimization fails here because element 6 (Type 2 for pair (1,11)) would map to the same pair as elements 7 and 10 (both Type 1-only), giving a fiber of 3, which violates the constraint. Let me verify whether (1,11) is actually the lexicographically minimal witness for x=6 by checking all pairs that sum to 12. I'm finding that the lexicographically minimal witness pair (1,11) is shared by all three values x=6, x=7, and x=10, giving a fiber of size 3, which contradicts the claimed bound of 2—so either I'm misinterpreting the KSS argument, the canonical pair is defined differently, or a different choice function is needed to achieve the fiber bound.

Actually, I realize I've been conflating ordered pairs with unordered pairs and mixing up witness pairs versus witness triples that include the collision element. For x=6 to be Type 1 with witness pair (1,11), I'd need some c∈S where 6+c=1+11=12, so c=6, but 6 isn't in S, so (1,11) doesn't actually witness x=6 as Type 1—x=6 is Type 2 instead, where 2×6=12=1+11. For x=7, the pair (1,11) works as a Type 1 witness since 7+5=12 and 5 is in S. Similarly, x=10 gives another Type 1 pre-image because 10+2=12. This means (1,11) has three pre-images total—one Type 2 and two Type 1—which violates the fiber bound, unless (1,11) isn't actually the canonical pair for x=6. I need to verify whether (1,11) is lexicographically minimal among all witnesses for x=6. The fiber of (1,11) under the lexicographic-minimum pairing contains three elements {6, 7, 10}, which contradicts the two-to-one property—but this might mean the lex-min choice isn't the right function, since the theorem claims there exists some function with fiber at most 2, not that every choice works.

For the specific example with S = {1,2,5,11} and A = {1,...,11}, I need to verify what witness pairs block each element in A\S, checking both additive and multiplicative constraints to see if a different pairing could achieve the required bound. For x=8, I'm checking each value of c: c=2 gives the pair (5,5), c=5 gives (2,11), and the Type 2 case yields (5,11), so the witness pairs are {(2,11), (5,5), (5,11)}. For x=9, I need to verify whether (5,5) works as a witness pair when c=1. I need to verify whether adding 9 to S creates any duplicate pairwise sums that would violate the Sidon set property, checking if any two distinct pairs sum to the same value. Adding 9 gives us {1,2,5,9,11} with all distinct pairwise sums, but when I try adding 3, I get a collision at 12 since both 1+11 and 3+9 equal 12, so that doesn't work. When I add 6 to get {1,2,5,6,9,11}, I find that 2+9=11=5+6, which is a collision. Adding 7 gives {1,2,5,7,9,11}, but now 2+7=9, creating another collision since 9 is already in the set. The set {1,2,5,9,11} is maximal—no other element from {3,4,6,7,8,10} can be added without creating a collision in pairwise sums. Now I'm analyzing this maximal Sidon set against the full range {1,...,11}, identifying which sums from S+S block each excluded element. I need to reconsider whether the set allows equal elements in the sum definition—the Lean specification permits a=b, which means 1+1=2 creates a valid pairwise sum, and since 1+9=10 equals 5+5=10 with {1,9} ≠ {5,5}, this is indeed a collision that disqualifies S from being Sidon. I'm checking if {1, 2, 5, 11} forms a valid Sidon set by computing all pairwise sums with repetition allowed, and confirming they're all distinct. Now I'm testing whether adding 3 works, but 1+2=3 creates a collision since 3 is already in the set. Checking if any other sums equal 5: 1+4 would work but 4 isn't in S, and 5+0 doesn't work since 0 isn't in S either. Moving through the remaining pairwise sums—3+5=8, 3+11=14—I find they're all distinct, confirming {1,2,3,5,11} is a valid Sidon set with 5 elements. Now I'm testing whether I can add 4 to the set, but 1+3 already equals 4, which would create a collision. For 8, I need to check if any pair sums to 8, but 5+8=13 collides with 2+11, so 8 can't be added. Similarly, 9, 10 all create collisions with existing sums. This confirms {1,2,3,5,11} is a maximal Sidon set within the range, and the remaining elements are 4, 6, 7, 8, and beyond. I realize I made an error—the set {1,2,3,5,11} is not actually Sidon because 1+3=4=2+2, which violates the condition since {1,3}≠{2}. I need to reconsider my approach.

Let me check {1,2,5,11} instead: all pairwise sums including self-sums are distinct, so this is Sidon. Now I'm testing whether it's maximal by trying to add other elements—adding 3 fails because 1+5=6=3+3, and adding 4 also creates a collision with 1+5=6=2+4. I've confirmed that {1,2,5,11} is a maximal Sidon set in the range 1 to 11 with 4 elements, and now I'm analyzing the complement set {3,4,6,7,8,9,10} to understand which elements can't be added and why by examining all possible pairwise sums. I'm checking whether 4 can be expressed as a sum from S, finding that (1,5) is the lexicographically minimal witness pair for x=3 since it's smaller than (2,2). Now moving to x=4, I'm testing Type 1 conditions by checking if various sums involving elements from S appear in S+S. For x=6, checking Type 2 reveals 12 is in S+S via (1,11), making it the canonical pair since it's lexicographically smaller than (2,5), so x=6 is Type 2. Moving to x=7, I'm testing whether 8 and 9 appear in S+S through various c values. For x=8, I'm finding multiple witness pairs: (5,5) from Type 1, (2,11) from Type 1, and (5,11) from Type 2, with (2,11) being lexicographically minimal, but this means x=8 is actually both Type 1 and Type 2 since it has witnesses from each category.

Since x=8 satisfies the Type 2 condition (2·8 = a+b for some a,b ∈ S), it gets classified as Type 2 overall and goes into the blocked set, even though its canonical pair (2,11) is a Type 1 witness.

Now checking x=9: it's Type 1 via (5,5) since 10 = 5+5, but 11 isn't in S+S so that doesn't work. For x=10, I check Type 1 pairs with various multipliers and find that 12 is in S+S as 1+11, giving the canonical pair (1,11). Type 2 doesn't work since 20 isn't in S+S. Now I'm tallying all the canonical pair assignments across the values I've checked. I'm checking the fiber count for pair (1,11) and finding it equals 3 with witnesses x=6, x=7, and x=10, which violates the expected bound of 2. After verifying each element's canonical pair, they all map back to (1,11), confirming this violation. The definition requires finding some function that maps elements of A\S to pairs in S×S where each pair has at most 2 pre-images, not necessarily using the lex-min canonical pair. Since (1,11) is the only pair in S×S that witnesses x=10, I can't reassign it to reduce the fiber—the constraint is fundamentally violated by this particular element. I'm computing all pairwise sums from S to get {2, 3, 4, 6, 7, 10, 12, 13, 16, 22}, then checking which values of the form 10+c fall in this set—only 10+2=12 works, giving the pair (1,11)—and verifying that 2·10=20 isn't in S+S either.

Since both x=10 and x=7 have only (1,11) as a witness pair, any valid map must send them both to (1,11), but x=6 has two options: (1,11) or (2,5), so I'm exploring whether assigning f(6)=(2,5) instead would keep the fiber sizes manageable.

If I send x=6 to (2,5), then (1,11) gets fiber {7,10} with size 2 and (2,5) gets fiber {6} with size 1—both acceptable. This means the lexicographically minimal choice doesn't work, but a strategic assignment that balances fiber sizes does, which reframes this as a matching problem where I need to assign blocked elements to witness pairs such that no pair receives more than 2 assignments.

Using Hall's theorem or a capacity-based approach, such a balanced assignment exists if and only if for every subset of blocked elements, the number of available witness pairs is at least half the subset size. I should revisit the KSS argument more carefully—it might be working in the integers rather than naturals, or it might be fundamentally about proving the existence of a good assignment rather than relying on a greedy choice function.

Looking back at the 1975 KSS paper, the key lemma states that if S is a maximal B₂-set in some set A of integers, then the number of elements outside S is bounded by 2|S|². The proof works by assigning each blocked element x a witness pair from S whose sum equals x plus some element of S, then using a charging argument to show this limits how many elements can be blocked. I need to clarify the distinction between working in ℤ versus ℕ—when x = (a+b)/2, we'd need x itself to be in S for this to work as a valid witness, but x isn't in S by construction, so this decomposition fails. The blocking condition requires that for any representation x + c = a + b with distinct pairs, at least one element must be x, and I'm checking cases where x combines with elements from S. For case 1, since x must be outside S while a, b, c are all in S, we have x ∉ {a,b}, and the equation x + c = a + b determines x uniquely from the pair {a,b} and witness c ∈ S. For case 2, x = σ/2 is uniquely determined by the sum σ. So for each minimum-sum canonical pair, at most one x can be charged to it. So x₂ + c₁ equals σ' which is less than σ, and if σ' can be expressed as the sum of two elements from S, then that pair witnesses x₂ through c₁.

This would contradict the minimality of σ unless x₂ + c₁ isn't in S + S, or unless x₂ + c₁ equals 2x₂ (which would require c₁ = x₂, but that's impossible since c₁ is in S and x₂ isn't).

Let me verify with the counterexample: S = {1,2,5,11}, x₁ = 10, x₂ = 7, canonical pair (1,11) with σ = 12. Here c₁ = 2 and c₂ = 5, so x₂ + c₁ = 9, which isn't in S + S = {2,3,4,6,7,10,12,13,16,22}. So the argument breaks down.

The fiber for (1,11) is actually 2 in this case, with x = 7 and x = 10 both Type 1-only, while x = 6 can be reassigned to (2,5). The construction still works, but the minimum-sum choice doesn't guarantee fiber ≤ 1 for Type 1-only elements. The fiber does seem bounded in practice, but the reasoning is more intricate than I initially thought, and I'm starting to suspect there might be a deeper obstacle to eliminating this axiom altogether.

Rather than relying on the lex-min/sum-min approach, I should try proving the cardinality bound |A\S| ≤ 2|S|² directly without invoking a 2-to-1 map. My example gives |A\S| = 7 and 2|S|² = 32, so the bound holds comfortably, but I need an actual proof. A bipartite graph approach using Hall's theorem might work here. Each element in T has at least one witness pair, and since each pair can witness at most |S| + 1 elements, we get |N(T)| ≥ |T| / (|S| + 1), but this only gives us the bound we need when |S| ≤ 1, which isn't useful.

The bipartite matching approach isn't working either, so I need to try something different. Let me use a charging scheme where I assign each blocked element to a witness pair more carefully. For Type 2 elements where 2x = a + b, the pair is unique in a Sidon set, so each pair gets charged at most once. For Type 1 elements, I need to figure out how to charge them to avoid overcounting.

But my example shows that a single pair like (1,11) receives multiple Type 1 charges (from x=7 and x=10), which breaks the bound. This suggests either my charging rule is wrong or the definition of Type 2 in the KSS paper is broader than I thought—maybe it includes cases I'm currently classifying as Type 1. Suppose two distinct elements x₁ and x₂ are both charged as Type 1 to the same pair {a,b} with sum σ, meaning x₁ = σ - c₁ and x₂ = σ - c₂ for distinct c₁, c₂ ∈ S. Since neither can equal σ/2 (which would require x = c, contradicting x ∉ S), and assuming c₁ < c₂


The lex-min canonical pair approach FAILS — The user worked through a concrete example with S = {1,2,5,11} in A = {1,...,11} and found that three elements (x=6, x=7, x=10) all have canonical pair (1,11) under lex-min, giving a fiber of size 3.

The minimum-sum approach also fails — Same issue: multiple Type 1-only elements can share the same minimum-sum pair.

But a strategic non-canonical assignment works — x=6 (Type 2) can be reassigned to (2,5) instead of (1,11), leaving (1,11) with fiber {7,10} of size 2. So the 2-to-1 map EXISTS but isn't the lex-min one.

The real insight: This is fundamentally a matching problem — we need to distribute blocked elements across witness pairs such that each pair receives at most two elements, which is always possible through careful reassignment rather than canonical selection.

Key observations: Type 2 elements have flexibility in their witness pairs, while Type 1-only elements are constrained but collectively have enough alternative pairs available. The strategy is to prove a balanced assignment exists via Hall's theorem by showing any subset of blocked elements has sufficient witness pairs to accommodate them.

For the Lean proof, I can't rely on a simple canonical pair function—instead I need either a Hall's theorem argument demonstrating the matching exists, a counting argument that avoids explicit construction, or a carefully chosen non-canonical map that provably satisfies the 2-to-1 property.

For option (c), the key insight is mapping Type 2 elements injectively by sending each x to its unique pair (a,b) with 2x = a+b, while for Type 1 elements I'd map x to a pair involving the maximum c ∈ S where x+c ∈ S+S, ensuring the overall fiber size stays bounded by 2.

The KSS paper's approach with B₂ sets is cleaner: for any maximal Sidon set S in A, each element outside S must create a repeated sum when added to S, which lets us construct an injective map from A\S into S×S with controlled fiber sizes. If x₂ + c₁ is not in S+S, then x₂ would need to be blocked by some other pair with sum less than σ, which again contradicts minimality. For Type 2 cases or mixed types, similar arguments show at most two elements can map to the same σ value. If two Type 1-only elements both map to σ with different constants c₁ and c₂, then swapping the constants gives sums either above σ or equal to σ plus the difference, which contradicts the minimality of σ as the smallest sum in S+S. I'm reconsidering the approach entirely—instead of constructing a 2-to-1 map, I should try proving the cardinality bound |A\S| ≤ 2|S|² directly by counting blocking triples (a,b,c) where a+b-c lands outside S, since each triple witnesses at most one such element.

The challenge is that while there are |S|³ possible triples, each element x ∈ A\S could be witnessed by many triples, giving only a cubic bound. To get the quadratic bound, I'd need each x to be witnessed by at least |S|/2 triples, but that's not guaranteed—some elements might only appear once.

Let me reconsider the minimum sum approach: mapping each x to the smallest sum that witnesses it, then analyzing how many elements can share the same minimum sum value. For a fixed sum σ, Type 2 contributes at most one element (x = σ/2), while Type 1 elements x = σ - c for c ∈ S are candidates, but σ(x) = σ only if σ is the minimum sum for x. If two Type 1 elements x₁ and x₂ both achieved σ with different witnesses c₁ and c₂, I can derive a contradiction by examining x₁ + c₂, which would create an alternative representation of σ that violates the Sidon property.

Since the minimum-sum approach doesn't cleanly bound the fiber for Type 1, I'm trying a different strategy: mapping each element x to a pair (c, σ) where c ∈ S and σ = x + c lies in S+S. This map is injective because if two pairs are equal, their first coordinates must match, which forces the second coordinates to match as well.

The codomain S × (S+S) has size at most |S|³, which is too large. But if I restrict to the minimum c that witnesses each x, then each pair (c, σ) has at most one preimage, and each c appears in at most |S+S| ≤ |S|² pairs, giving a total bound of |S|³ — still not the |S|² I need.

The issue is that using separate dimensions for c and σ doesn't compress enough. The KSS argument achieves a 2-to-1 map, so I should reconsider what makes that work. Looking at the strategy document, the lexicographic-minimum canonical pair overshoots the fiber bound, the min-sum approach doesn't provably keep fibers ≤ 1 for Type 1 sumsets, but the concrete example shows a non-canonical assignment that does achieve fiber ≤ 2.

This is fundamentally a matching problem: I can model it as a bipartite graph where one side is A\S and the other is S×S, with edges connecting x to pairs (a,b) that witness it. The key is showing this graph satisfies a Hall-type condition—for any subset T of A\S, the neighborhood N(T) must be large enough relative to T. If I can establish that |N(T)| ≥ |T|/2 for all T, then a generalization of Hall's theorem guarantees a 2-to-1 map exists. Each pair can witness at most |S| + 1 elements, so the bound |N(T)| ≥ |T|/(|S|+1) is too weak to guarantee Hall's condition when |S| > 1. I should abandon this matching approach and instead use a direct double-counting argument to bound |A\S| by 2|S|². I see that a pair (a,b) can witness up to |S|-1 elements total, so the naive per-pair bound fails. The key insight is to count only one canonical witness pair per element x, which means I need a map from A\S to S×S where each pair appears in at most 2 fibers — and the lex-min choice doesn't achieve this, so I need either a different canonical selection or a different approach entirely.

For the Sidon property approach: since S is Sidon, each sum a+b appears at most once, which limits how many Type 1 elements can share the same witness pair (c, a+b). For a fixed c, the map sending x to x+c is injective, so I can bound the number of Type 1 elements witnessed by pairs involving c.

However, this still gives a cubic bound when I sum over all possible c values, since each sum in S+S contributes at most |S|² elements. The real challenge is that different elements of A\S might require different witnesses, so I can't easily avoid the union over c. A better approach might be to use that each x ∈ A\S must have multiple witnesses—if I can show each element is witnessed by at least k triples, then double counting gives |A\S| ≤ |S|³/k, though guaranteeing k is large seems difficult.

Let me reconsider by partitioning A\S directly: I already have |Type 2| ≤ |S|² from the map x ↦ 2x, but |Type 1 only| still appears to be cubic. The key question is whether I can tighten |Type 1 only| ≤ |S|², which would require showing that elements in A\S that only satisfy x + c = a + b (but not 2x = a' + b') are somehow constrained to a quadratic set. I'm realizing the circular reasoning here—I need to step back and look at what the KSS paper actually argues rather than trying to reconstruct it myself, since the direct counting approach seems to rely on structural properties of collisions that I'm not capturing with the pair-counting method. I need to reconsider the blocking structure more carefully — each sum from a pair in S can block multiple elements, but the counting argument isn't straightforward. Let me focus on what's actually provable in Lean rather than getting stuck on the theoretical bound, and revisit the user's key insight about how Type 2 elements like x=6 might be reassigned in their example. For Type 1-only elements, I need to find an injection into S×S, but canonical choices like lexicographic minimum don't work, and alternative mappings either exceed the codomain bounds or map into larger sets like S × (S+S) instead of staying within S×S. I'm defining a function that maps each Type 1 element x to the pair (a,c) from its lexicographically smallest witness triple, but I need to verify this is actually injective—if two different x values map to the same (a,c), their corresponding b values could differ, which would make them distinct elements mapping to the same output. If x₁ < x₂, then b₁ < b₂, but since f evaluates each x independently, the lex-minimality condition for each x is determined separately—so different b values don't create a collision. I should reconsider whether f is actually injective by defining it as just the pair (a_x, c_x) instead.

Wait, if two different x values map to the same (a, c), they'd satisfy x₁ + c = a + b₁ and x₂ + c = a + b₂ with b₁ ≠ b₂, meaning both x values would be in the preimage of (a, c)—so the map isn't injective. But if I map x to (a + b, c) instead, that's injective because x uniquely determines the sum σ = a + b, and I can recover x = σ - c.

The issue is the codomain should be (S + S) × S, not S × S. When I try mapping to just (a, b), the pair determines σ but different x values with the same (a, b) would need different c values, so the fiber over any (a, b) has size at most |S|—still not injective.

I'm stuck in a loop trying to construct an explicit injection. Let me abandon this and instead bound the size directly: Type 2 elements are at most |S + S| ≤ |S|², and for Type 1 only elements, I should be able to show there are at most |S|² of those as well.

For Type 1 only, each element x has a blocking equation x + c = a + b with a, b, c ∈ S, so x lies in (S + S) - S. This set could be as large as |S|³ naively, but the Sidon property should tighten this further.

Since S is Sidon, S + S has exactly k(k+1)/2 distinct elements. The set of differences {σ - c : σ ∈ S+S, c ∈ S} that avoid S has at most k³/2 elements before filtering, but if each difference appears in at least k/2 pairs, we can bound |Type 1 only| by 2k².

The key insight is that for a Type 1 element x blocked by sum σ, the Sidon property ensures σ uniquely determines the pair {a,b} with a+b = σ, so x = σ - c is the only way to express x as a difference from that particular sum. For a Sidon set where σ = a+b, the elements σ-a = b and σ-b = a are both in S, so at most |S| - 2 other elements c can have σ-c ∉ S, meaning Type 1 elements blocked by σ number at most |S| - 2. must have σ-c ∉ S, because if σ-c were in S as some element d, then a+b = c+d would give us two distinct pairs with the same sum, violating the Sidon property. For each pair {a,b} with sum σ, the blocked elements are at most {σ-c : c ∈ S \ {a,b}, σ-c > 0, σ-c ∈ A}, giving at most |S|-2 elements per pair and a cubic bound overall. Since this raw counting approach isn't yielding improvement, I should try a different strategy—perhaps mapping to unordered pairs or finding a better way to bound the Type 2 blocked elements instead.

Actually, I can use an injection: for each Type 1 element x where x+c = a+b with a,b,c ∈ S, the map x ↦ (c, σ) with σ = a+b is injective because the Sidon property uniquely determines {a,b} from σ and x from (c, σ). The codomain is S × (S+S), which has size k · k(k+1)/2.

But this still gives |S|³, which isn't tight enough. I'm wondering if I should try a different approach entirely—maybe looking at what the actual KSS proof uses rather than continuing down this path.

Let me reconsider what I can actually construct in Lean. The strategy document shows that the naive canonical approach doesn't work cleanly because fibers can be larger than 2, and while a 2-to-1 map exists, it requires non-canonical choices. The real insight might be that certain elements like x=6 can simultaneously satisfy multiple type conditions, which could be the key to getting the quadratic bound. Also Type 1 blocked. So 6 is blocked by both conditions: doubling it gives 12 which is in S+S, and adding it to 1 gives 7 which is also in S+S. The key insight is that x=6 qualifies as both Type 1 and Type 2, with different witness pairs—assigning it to its Type 1 pair (2,5) instead of the lex-min Type 2 pair (1,11) allows for better fiber size distribution. This suggests the construction should prioritize Type 1 pairs for elements that satisfy both conditions, then handle Type 2-only and Type 1-only elements with their respective pair assignments. I realize the Type 1 only approach with (a,c) doesn't work since different x values can map to the same pair. Instead, I should map x to (a+b, c), which uniquely determines x = (a+b) - c, and since Sidon sets have unique representations, I can encode the sum a+b as a pair in S×S to fit the required codomain.

However, this gives a codomain of size |S|³, not |S|², because c isn't determined by (a,b). The key insight from KSS is that for Type 1-only elements, I need to use the canonical pair that minimizes the sum a+b specifically among Type 1 witness pairs, not among all possible witnesses. I need to check whether x₂+c₁ is in S+S to derive a contradiction—if it is, then x₂ would have a smaller sum witness, but if it isn't, the argument breaks down. The key insight is that considering three Type 1-only elements sharing the same canonical pair forces cross-sum constraints that would make at least one of them Type 2, which resolves this issue. I'm checking whether cross-sums like x₂ + c₁ fall in S+S, which would indicate a Type 2 constraint, and examining how the ordering of the c_i values determines which cross-sums exceed or fall below σ. The cross-sums x_i + c_j where i ≠ j all equal σ plus or minus differences of the c values, giving three sums below σ and three above it. For the pairwise sums from A\S like x₁ + x₂ = 2σ - c₁ - c₂, I need to determine whether these fall in S+S.

I'm realizing I've been going in circles—the core issue is that proving a 2-to-1 map requires a non-canonical choice, so I can't just define it directly. Instead, I should either find a direct bound on |A\S| using a different argument like averaging, construct a stronger canonical injection into S×S, apply Hall's theorem to show the 2-to-1 map exists, or explore another approach entirely.

Option 4 looks promising: injecting type1Only into S+S by mapping each element x to x plus its minimal partner in S, which would immediately give the bound |type1Only| ≤ |S|². The key question is whether this map is injective—if two elements map to the same sum, they'd need to have different minimal partners, which seems like it could lead to a contradiction. If x₂ + c₁ is in S+S, then c₁ would be a smaller witness for x₂, contradicting the minimality of c₂, so φ is injective only when this element falls outside S+S. Since c_min(x₂) = c₂ is the smallest element of S where x₂ + c₂ ∈ S+S, if c₁ < c₂ and c₁ ∈ S, then c₁ + x₂ cannot be in S+S by the minimality condition — which gives me the contradiction I need to show φ is injective. ∉ S+S, which contradicts c₁ being in S and x₂+c₁ equaling σ ∈ S+S. This contradiction means φ(x₁) ≠ φ(x₂), so φ is indeed injective. I'm working through a concrete example with S = {1,2,5,11} to verify that φ is indeed non-injective: both x₁ = 10 and x₂ = 7 map to φ = 12, since 10+2 = 12 and 7+5 = 12 are the minimal sums in S+S for each input.

The pair (c_min(x), φ(x)) would be injective since x = φ(x) - c_min(x) uniquely determines x, but the codomain S × (S+S) becomes too large with |S|³ elements, so I need a different approach to find a canonical injective function.

Let me try using the maximum sum instead: for each x, I find the largest value in S+S that can be expressed as x+c for some c ∈ S. But x=7 and x=10 both give max sum 12, and even restricting to Type 1 witnesses doesn't help since they still collapse to the same value.

The core issue is that I need to bound |Type 1 only| by |S|², but my attempts to map these elements injectively into S+S keep failing because multiple blocked elements can witness the same sum.

Instead, I should characterize Type 1 only elements more carefully: for each sum σ in S+S with unique representation σ = a+b, the Type 1 only elements are exactly those of the form σ-c where c is in S but not in {a,b}, and σ-c itself isn't in S and doesn't double to something in S+S. Since the Sidon property guarantees σ-c ∉ S for c outside {a,b}, I can focus on the constraint that 2(σ-c) ∉ S+S to count these elements.

The total count involves summing over all sums, but this creates overcounting since a single element x can be represented as σ₁-c₁ and σ₂-c₂ for different sums. The key is tracking how many distinct sums each element x participates in, which determines the actual contribution to the total. I realize the KSS paper actually works over the integers rather than natural numbers, which means negative elements are involved and the blocking equations take different forms—like x = a + b - c or 2x = a + b, plus equations involving differences like x - c = a - b. The Sidon property in ℤ concerns when sums a+b equal c+d with distinct pairs, and blocking x involves equations of the form x + s₁ = s₂ + s₃ for elements in S.

Actually, the B₂ condition is fundamentally the same in ℤ as in ℕ—the blocking equations don't change meaningfully. The difference might just be that ℤ has more elements to consider and different counting arguments apply, but the core approach should work similarly.

I should look more carefully at what the KSS paper actually does. Rather than getting stuck on proving the exact 2-to-1 property, maybe I should consider whether an approximate bound like |type1Only| ≤ C·|S|² for some constant C would be sufficient instead.

Since I already have |type1Only| ≤ |S|³, which gives the cubic bound, the real challenge is tightening this to quadratic. The key insight is that for each x in type1Only, I can use the Sidon property to show there are many witness sums—that is, many values c ∈ S where x+c lands in S+S—and this multiplicity should force the count down. can't guarantee |W(x)| ≥ 2, so I need a different approach—maybe looking at the structure of type1Only elements more carefully or finding a contradiction when too many elements lack witnesses in S+S. Looking at the three pairwise sums of x₁, x₂, x₃, none of them lie in S+S since both summands are outside S. The key insight is checking whether 2x₁ = 2σ - 2c₁ belongs to S+S—if it does, then x₁ is Type 2, and I need to determine if at least one of the three elements must satisfy this condition.

Actually, the stronger argument comes from considering when x₁ and x₂ share the same minimum-sum witness σ_min, meaning x₁ + c₁ = σ_min and x₂ + c₂ = σ_min with c₁ ≠ c₂. Then x₂ + c₁ would equal σ_min minus the difference between c₂ and c₁, which creates a constraint that forces at least one element into Type 2. When x₂ is inserted into S, it must create a collision of the form a+b=c+d where x₂ is one of the elements, which means either x₂ + s₁ = s₂ + s₃ for elements in S (Type 1), or 2x₂ = s₁ + s₂ (Type 2). Since we established x₂ + c₁ isn't in S+S, the only way x₂ can be witnessed is through a different element s₁ ∈ S where x₂ + s₁ ∈ S+S. The minimum-sum approach doesn't work for counting pairs, so I'm trying a different mapping: instead of pairing elements, I'll map each type1Only element to its minimum sum σ_min(x) in S+S. For a given σ, at most |S|-2 type1Only elements can achieve that minimum sum, since each would correspond to a different c ∈ S where x+c = σ and x ∉ S. For a fixed σ, the type1Only elements are those of the form σ-c where c ∈ S \ {a,b}, σ-c > 0, σ-c is type1Only, and σ is the minimal witness sum for σ-c. The minimality condition requires that for all c' < c, we have σ-(c-c') ∉ S+S, which is difficult to work with directly. Let me step back and think about whether we can bound |type1Only| ≤ |S|² using a different approach.

The strategy document explores many avenues but hits the same wall repeatedly: the natural injection into S × (S+S) gives |S|³, we can't reduce to S+S alone because multiple elements map to the same sum, canonical pairs have fibers larger than 1, and double counting doesn't yield the right constant. At this point, it seems the axiom might be essential for a clean proof, and removing it would require genuinely new mathematical techniques.

Rather than persisting with the 2-to-1 map approach, I should try proving |A\S| ≤ 2k² directly without constructing such a map. Using additive energy and Cauchy-Schwarz might offer a path forward here.

For a Sidon set, the additive energy counts 4-tuples where a+b = c+d, which forces {a,b} = {c,d}. This means each unordered pair contributes exactly 2 ordered pairs to the count, giving E(S) = k² total. There are C(k,2) sums from distinct pairs contributing 4 each and k diagonal sums contributing 1 each, giving E(S) = 2k² - k. Now I'm trying a Cauchy-Schwarz approach: for each sum σ, I'll count how many type1Only elements have a witness in S, and since each element needs at least one witness, the total witness pairs must be at least |type1Only|. Substituting back, I get |type1Only|² ≤ |S|⁶, so |type1Only| ≤ |S|³—still cubic. The Cauchy-Schwarz bound doesn't improve things, and achieving quadratic would require each element to have many witnesses, which my counterexample shows isn't guaranteed.

I suspect the KSS proof uses a more refined argument I'm missing. Rather than continuing to chase this, let me focus on what's actually achievable: the lex-min canonical pair fails because fibers exceed 2, and any 2-to-1 map requires non-canonical choices. I'll try one more angle by mapping type1Only elements to ordered triples in S³ and counting those triples directly. For each value x, the witnesses c ∈ W(x) correspond to ordered triples, giving at least |W(x)| triples total. This bound still isn't tight enough, so I'm switching to a different approach: characterizing type1Only as differences σ - c where σ is a sum in S+S, c is in S, but σ - c is not in S. By the Sidon property, when c is outside the pair {a,b} that forms σ, the difference σ - c falls outside S, meaning these differences are exactly the type1Only elements. If two different pairs (σ₁, c₁) and (σ₂, c₂) produce the same difference x, then σ₁ - σ₂ = c₁ - c₂, which means a₁+b₁+c₂ = a₂+b₂+c₁—a constraint involving three-element sums from S that Sidon sets don't typically satisfy.

I'm not finding a productive way forward with this approach, so let me try proving the bound directly by contradiction: if more than 2k² elements are blocked, the pigeonhole principle forces some sum to block multiple elements, which should lead to a contradiction with the Sidon property.

However, even accounting for the maximum blocking capacity per sum, the arithmetic doesn't quite force the contradiction I need. I think I've explored the main avenues here and should give the user an honest assessment of what I've found and where the gaps remain. I need to decide between accepting an axiom, proving a weaker bound, formalizing Hall's theorem for a complete proof, or reading the KSS reference to understand their actual argument—and I think the most honest approach is to read the reference, identify the precise claim needed, and then assess what's feasible to formalize rather than spending hours on dead ends.

The core insight is that I need to construct an injection from type1Only elements to pairs in S², and I'm starting to see how to do this by examining the structure of elements where x + c = a + b. I'm considering whether mapping x to both its minimum sum and the corresponding element from S gives an injective map into a quadratic-sized space, but this seems to require |S|³ elements. To reduce to quadratic, I'd need the minimum sum to determine the element or vice versa. Let me instead try projecting onto S+S and bounding the fiber size for each sum by showing that for a fixed σ ∈ S+S (which has a unique representation as a+b in a Sidon set), the preimage consists only of elements where σ is the minimal witness sum. For three elements in the fiber to all have the same minimum witness sum σ, the smallest element x₃ = σ-c₃ would need σ-(c₃-c') ∉ S+S for all smaller c' ∈ S, which creates a strong constraint that seems difficult to satisfy simultaneously for three distinct elements. For x = 99, I'm checking which sums 99 + c land in S+S to find σ_min(99). Since 99 + 1 = 100 isn't in S+S but 99 + 2 = 101 is, the minimum is σ_min(99) = 101 with c_min(99) = 2.

For x = 96, I'm doing the same: 96 + 1 and 96 + 2 don't work, but 96 + 5 = 101 lands in S+S, so σ_min(96) = 101 with c_min(96) = 5.

This gives fiber(101) = {99, 96} with size 2. I'm exploring whether I can add a third element by checking if 101 - 1 = 100 works, noting that 100 is in S, so it doesn't qualify as type1Only.

Switching to S = {1, 3, 7, 100}, I compute S+S and find σ = 101 = 1 + 100. For 98 = 101 - 3, the minimal σ is 101 (since 98 + 3 = 101 ∈ S+S), and similarly for 94 = 101 - 7, the minimal σ is also 101. This gives fiber(101) = {98, 94} with size 2, and I'm now trying to add another witness to 101.

Since any witness must be 101 - c for c ∈ S{1, 100}, only c ∈ {3, 7} work, so the fiber is capped at size 2. With |S| = 5, the maximum fiber per sum should be 3, so I'm testing S = {1, 3, 7, 20, 100} to see if this Sidon set produces larger fibers.

For σ = 101, the elements 98, 94, and 81 all have σ_min = 101, meaning they each have exactly one witness in S + S, confirming the fiber size is indeed 2 for this sum. All three elements in the fiber share the same minimum sum value, confirming the fiber isn't bounded—it can grow as large as the set itself. This means a direct injection into S+S won't establish the quadratic bound, so the KSS argument must rely on a more sophisticated approach than simple canonical mappings. I need to read the KSS reference and understand their actual argument for the 2-to-1 mapping, then attempt to formalize it—or if that proves too difficult, pivot to finding an intermediate bound using a different counting approach. The user wants action, so I'll start by studying the reference and then work through what's formalizable